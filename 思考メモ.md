# 27. Remove Element  
## step1  
順々にnumsの要素を先頭から見て行ってvalと同じものはpopすることを思いついた  
しかし,先頭に詰めるのはどうすればよいか分からない  
詰めなくてもよさそうか？  
しかし,個々の要素のカウントはどうすればよいのか  
list関数を使えばよさそう  

それらから以下を書き下した
```
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        for i in range(len(nums)):
            if nums[i] == val:
               nums.pop(i-1)
            
        return len(nums)
```
しかしpopの影響で見るindexが一つずれてしまうため通らない

ほかに思いつかなかったのでsolutionを見てみる  
・https://leetcode.com/problems/remove-element/solutions/5468263/video-step-by-step-explanation-by-niits-eyte/

以下のコード
```
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        k = 0

        for i in range(len(nums)):
            if nums[i] != val:
                nums[k] = nums[i]
                k += 1
        
        return k
```
流れとしてはこうだ  
1.kを違う文字の個数として置く  
2.valと違う値を見つけた場合にはkの指す位置にnums[i]を入れる  
3.kを+1する  
4.kを返す.これはvalと等しくない要素の個数になっている

## step2  
先駆者がいなかったので様々なsolutionsを見てみる  
・https://leetcode.com/problems/remove-element/solutions/3670940/best-100-c-java-python-beginner-friendly-2ve8/  
iをindexにしている.indexを返したいのでこれでよさそう  

・https://leetcode.com/problems/remove-element/solutions/7263332/clean-simple-beats-easy-to-understand-ef-awxz/  
remove()メソッドを使用する。これは配列から対象の値を削除したのちに削除した値が入っていたindexを詰める  
ちなみに時間計算量は配列を走査(O(n))したのちに対象を削除して詰める(O(n))  
したがってO(n^2)  
空間計算量は,追加で使用しているメモリはないためO(1)  

他のも見てみたが大体同じだった

## step3  
行数が少ないことによる見やすさ、nums.remove(val)という名前からすぐに処理の内容がわかること  
これらのことから以下のコードを採用する  
```
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        while val in nums:
            nums.remove(val)
```
